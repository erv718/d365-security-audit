# CLAUDE.md

Guidance for AI assistants working in this repository lives in [AGENTS.md](AGENTS.md). Read it
first and follow it. The two rules that matter most: the tool only reads, and `output/` and
`.env` hold real tenant data that must never leave the user's machine.
